# Notes

## `manifest.json`

This *must* be in the root folder of the directory! It's how Chrome knows the folder is an extension.

- `"default_icon"`: equivalent to a favicon
- `"default_popup"`: equivalent to an index.html page

# Vocabulary

action badge::a banner, on top of the extension action (toolbar icon)
